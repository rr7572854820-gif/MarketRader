# MarketRadar — Session Journal

This is the project's running development journal. It is not a status dashboard and not a changelog — it is a record of what actually happened in each working session, including the reasoning, the mistakes, and the open threads, so that the next session (human or Claude) can pick up with full context instead of re-deriving it.

**Update this file at the end of every development session.** A session that isn't logged here didn't happen, as far as future sessions are concerned. See [CLAUDE.md](./CLAUDE.md) §13 for documentation standards this file should follow.

Do not delete old entries. This file is additive — append new sessions above or below consistently (this project logs newest-first) rather than overwriting history. If the file grows unwieldy, that's a future problem to solve deliberately (e.g., archiving old entries to a dated file), not a reason to delete the record silently.

---

## How to Fill Out a Session Entry

Copy the template below for each new session. Leave a section explicitly empty (e.g., "None this session.") rather than deleting it — a consistently structured empty section is more useful than a missing one.

```
## Session — YYYY-MM-DD

### Current Objective
What this session set out to do, in one or two sentences.

### Completed Work
What was actually finished this session — concrete, verifiable outcomes.

### In Progress
What was started but not finished, and its actual current state.

### Known Issues
Anything broken, wrong, or unresolved that the next session needs to know about.

### Next Tasks
The specific, concrete next steps — written so the next session can start immediately without re-deriving priorities.

### Important Decisions
Any real decision made this session, with the reasoning behind it — especially anything that future sessions must not silently re-litigate or contradict without reason.

### Questions
Anything left genuinely open or unresolved — for the user, or for a future session to investigate.

### Lessons Learned
What this session revealed that changes how future work should be approached — process, not just product.

### Future Improvements
Ideas worth considering later, explicitly not committed to now.
```

---

## Session Log

## Session — 2026-07-30 (Task 7: Configuration & CLI Polish)

### Current Objective
Improve usability, configuration management, and CLI experience without changing core pipeline behavior. Explicitly required inspection first (config.py, pipeline/, CLI runner, env handling, factories, tests) and a stated plan before any changes — followed that structure directly in the response rather than treating it as a formality.

### Completed Work
- **Inspection found two real, empirically-confirmed bugs in `src/config.py`, not hypothetical gaps.** (1) Tested directly: invoking code from outside the project root silently failed to find `.env` at all, with `gemini_configured`/`reddit_configured` both silently coming back `False` despite real credentials existing — `python-dotenv`'s implicit upward-search behavior turned out to be exactly the kind of "unpredictable between invocations" behavior the task's own "predictable between development and production" objective was naming. (2) By inspection: `_optional()` only checked truthiness, so `GEMINI_API_KEY="   "` (whitespace-only, e.g. left over from editing `.env`) would have been treated as "configured."
- Fixed both in `src/config.py`: `.env` is now loaded from an explicit, fixed path (`Path(__file__).resolve().parent.parent / ".env"` — the project root, regardless of current working directory), and all env values are now read through one helper (`_env_str`, replacing `_optional`) that strips whitespace and treats blank-after-stripping as unset. Re-verified the cwd fix empirically the same way the bug was found: ran `load_config()` from a different working directory before and after the fix.
- **Made `load_config()` properly testable without touching real secrets.** Added an optional `dotenv_path` parameter (default: the real project `.env`, so every existing caller is unaffected) — without this, tests would either leak real `.env` values into assertions or have `load_dotenv()` silently repopulate variables a test tried to clear, since `override=False` is `python-dotenv`'s default and `.env` genuinely has real credentials in this project.
- Wrote `tests/test_config.py` — 6 tests: defaults with nothing configured, real values read from a temp `.env`, whitespace-only values correctly rejected, partial Reddit credentials (id without secret) correctly not counted as configured, a missing `.env` file not raising, and — directly reproducing the bug that was found — config loading proven independent of current working directory via `monkeypatch.chdir`.
- Improved `src/pipeline/runner.py`: added `_positive_int` and `_non_blank` as `argparse` `type=` validators for `--limit` and `--subreddit`, replacing silent acceptance of `0`, negative numbers, non-numeric strings, and blank subreddit names with clean, specific, one-line errors (`argparse`'s standard exit code 2, no tracebacks) — verified directly against all four bad-input cases plus the still-working valid case. Expanded thin help text on every argument, added `prog=` for a cleaner usage line, and added an `epilog` with four concrete example invocations plus a one-paragraph "how configuration works" pointer, using `RawDescriptionHelpFormatter` to preserve its formatting. Inspected the rendered `--help` output directly rather than assuming the source read correctly.
- **The em-dash/Windows-console encoding bug recurred a third time**, this time in the new help text and epilog — caught by a blanket `grep` for the actual em-dash character across the whole file (a narrower, compound grep pattern used first missed several instances hiding on string-literal continuation lines) and confirmed fixed by actually rendering `--help` and grepping its real output for non-ASCII characters, not just the source.
- **Found and fixed a second, more interesting bug via testing, not inspection**: `logging.basicConfig()` silently no-ops if the root logger already has handlers configured — meaning `_configure_logging()` would only actually take effect the *first* time it was called in a process. Two new tests for verbose vs. non-verbose logging behavior (regression-protecting the Task 6 fix) caught this directly: the second test failed because the first test's `basicConfig` call had already "claimed" the root logger. Fixed with `force=True`, which makes the function correctly idempotent — a real production correctness fix (any long-lived caller invoking this twice would have hit the same silent failure), not just a test workaround.
- Added `tests/test_pipeline.py` coverage: `--limit` rejecting four categories of bad input (parametrized), `--subreddit` rejecting blank, `--help` exiting 0 and listing every flag, and the two logging-configuration tests above. Suite grew from 32 to 47 tests (6 config + 9 CLI/logging), all offline, all passing.
- **Addressed the README gap found during inspection**: the entire file was vision/narrative with zero setup or run instructions anywhere in the project's front door. Added a concise "Getting Started" section — prerequisites, install, `.env` configuration pointer, `check_connections.py` verification step, and the four canonical `runner.py` invocations from its own `--help` epilog, kept consistent rather than duplicated with slightly different wording.
- **Addressed the `analyze_preview.py`/`runner.py` naming confusion flagged as an open question in Task 6's SESSION.md entry** — conservatively: added a one-line docstring note to `analyze_preview.py` pointing to `src/pipeline/runner.py` as the maintained entrypoint, and a similar lighter note to `fetch_preview.py` clarifying its narrower, still-genuinely-useful scope (Fetcher-only diagnostics, no AI cost). Deliberately did **not** remove, rename, or restructure either file — doing so would risk exactly what "maintain backward compatibility with existing commands" and "do not rewrite working architecture" were explicit constraints against, for a question that was still open, not resolved, going into this task.
- **Ran a full backward-compatibility smoke test across every pre-existing entrypoint** (`check_connections.py`, `fetch_preview.py`, `analyze_preview.py`) after all changes, not just the new `pipeline.runner` — all three still exit 0 and behave identically to before. Bonus finding: the default Gemini model's daily quota appears to have reset since earlier sessions today — `analyze_preview.py`'s real-Gemini smoke test succeeded without needing the `gemini-flash-lite-latest` substitute for the first time this session.

### In Progress
None. Task 7 is complete: inspected first, plan presented before implementation (per the task's explicit structure), config and CLI bugs found and fixed with empirical verification (not just code reading), 15 new tests (suite now 47), README gap closed, naming confusion addressed without breaking anything, full backward-compatibility smoke test passed.

### Known Issues
- All Known Issues from the Task 3 through 6 entries below still apply except what this session directly addressed (the two config bugs, the CLI validation gaps, the logging idempotency bug, the README gap).
- `analyze_preview.py` still exists as a parallel, narrower path to much of what `runner.py` does — flagged again, not resolved, consistent with Task 6's own open question. Now has a docstring pointer at least.
- `--keyword` still accepts a blank string (`--keyword ""`) without validation — not fixed, since `MockFetcher`/downstream code already treats an empty keyword as "no filter" gracefully (falsy check), so there's no actual bug here, just an inconsistency with `--subreddit`'s stricter validation. Noted, not changed, to avoid validating something that was never actually broken.

### Next Tasks
- Whatever comes next per the user's direction — no task number was given to explicitly avoid, unlike every prior task in this session.
- If `analyze_preview.py`'s fate is ever decided (deprecate/remove vs. keep indefinitely), that's still a deliberate call for the user to make, not something to infer from "the CLI got polished."

### Important Decisions
- Followed the requested structure literally: inspected and presented a concrete plan (with a files-to-change table) before writing any code, in the same response — not treated as a formality to rush past.
- Fixed the `.env` cwd-dependence bug at the root (explicit path resolution) rather than only documenting it as a known limitation, unlike some earlier sessions' "name it, don't fix it" calls — this one was cheap, clearly in-scope for "predictable configuration behavior," and had zero architectural blast radius.
- Added `dotenv_path` as an optional `load_config()` parameter specifically to make real testing possible without either touching real secrets or writing tests that would silently do nothing useful — a small, backward-compatible interface addition in service of the task's own "add tests for new configuration behavior" requirement, not scope creep.
- Chose conservative documentation-only fixes (docstring pointers) over removing/renaming `analyze_preview.py`, even though "remove unnecessary friction" and "improve confusing naming" could be read as license to do more — "maintain backward compatibility with existing commands" and "do not rewrite working architecture" were explicit, and an unresolved open question from a prior session isn't consent to resolve it unilaterally now.
- Fixed the `logging.basicConfig` idempotency bug in production code (`force=True`) rather than only working around it in the test — the bug was real and would have affected any real caller invoking `_configure_logging` twice, not just the test suite.

### Questions
- All still-open questions from the Task 3 through 6 entries below remain open, plus: should `analyze_preview.py` be deprecated now, given it's been flagged twice (Task 6, Task 7) without a decision? Still not resolved unilaterally.

### Lessons Learned
- **Empirically confirming a suspected bug before fixing it, using the exact same method that will later verify the fix, is worth the extra two commands.** The cwd-dependence issue could have been "fixed" based on reading `python-dotenv`'s docs and reasoning about `usecwd`/`find_dotenv` internals — instead it was demonstrated directly (ran from a different cwd, watched it silently fail) and the fix was verified the identical way, leaving zero doubt either was real.
- **A narrow grep pattern can miss a bug the same class of bug already taught this project to look for.** The em-dash scan used a compound pattern (`grep -E 'print\(|help='` piped into a non-ASCII filter) that missed continuation lines of multi-line help strings — the third occurrence of this exact bug class was caught by finally just grepping for the literal character across the whole file, no cleverness. Simpler checks generalize better than pattern-matched ones.
- **New tests can find bugs the feature they're testing didn't know it had** — the two logging tests weren't written to find the `basicConfig` idempotency issue, they were written to regression-protect the Task 6 verbose-flag fix. The idempotency bug fell out of running them in sequence, which is exactly the kind of thing a single, isolated manual check would never have surfaced.

### Future Improvements
- If a `--keyword` inconsistency with `--subreddit`'s validation strictness ever becomes a real complaint, revisit whether blank keywords should be rejected too — not changed this session since nothing was actually broken.
- Once `analyze_preview.py`'s fate is decided, this is the natural session to look back at for what's safe to remove and what any migration should look like.

---

## Session — 2026-07-30 (Task 6: Automation Pipeline)

### Current Objective
Build `src/pipeline/` (`pipeline.py`, `runner.py`) so the entire Fetch → Analyze → Cluster → Verify → Report sequence runs with a single command, with configuration options, real logging, retry/graceful error recovery, and a saved execution summary. This is effectively "Task 8: CLI entrypoint" from TODO.md's own renumbering scheme, flagged as overdue since Task 5. Stop before Task 7.

### Completed Work
- **Resolved a real architectural tension before writing code.** Task 6 needs `--mock` and an AI-provider override, which requires bypassing the factories' automatic config-based selection — in direct tension with the established "never construct `MockFetcher`/`MockAIProvider`/`RedditFetcher`/`GeminiProvider` directly outside the factory" rule (Tasks 2 and 3). Resolved by extending `get_fetcher()` and `get_ai_provider()` with a new `force_mock: bool = False` keyword-only parameter — backward compatible (every existing caller, unaffected), and keeps the override capability *in* the factory rather than working around it. Regression-tested immediately (all 20 prior tests still passed) before building anything on top.
- **"Number of AI calls" had nothing counting it anywhere in the codebase.** Rather than instrumenting `GeminiProvider` itself (would touch already-reviewed Task 3 code), wrote `_CountingAIProvider` in `src/pipeline/pipeline.py` — a pure decorator implementing the `AIProvider` interface, wrapping whichever real provider gets resolved, incrementing a counter on every `generate_text` call. Transparent to `Extractor` and `Aggregator`, which already accept "any `AIProvider`" and have no idea they're wrapped. This turned out to give a *more* accurate count than originally assumed — it naturally includes `Extractor`'s own internal retry-on-malformed-JSON calls (Task 3), not just "one call per post," which a test caught directly (see Bugs).
- Built `src/pipeline/pipeline.py`: `PipelineConfig` (subreddit, keyword, post_limit, output_dir, ai_provider ["auto"/"mock"], report_format ["terminal"/"markdown"/"both"], force_mock_fetch), `PipelineExecutionSummary` (all of requirement 5's fields, saveable, not just logged), `PipelineRunResult` (summary + the actual `InsightReport`), and the `Pipeline` class. Deliberately a pure library — no `print()` calls anywhere in it, using the standard `logging` module for operational visibility instead, consistent with the project's existing rule that only presentation-layer code (`formatter.py`, CLI scripts) prints.
- **Reliability, concretely:** fetch now has its own retry loop (`_fetch_with_retry`, 3 attempts, exponential backoff) — the one pipeline stage that didn't already have retry (AI calls have retried inside `GeminiProvider` since Task 3). Extraction failures stay per-post (one bad post never aborts the run — already true since Task 3, preserved here). `Pipeline.run()` has exactly one broad `except Exception` in the entire codebase — a deliberate, documented top-level safety net (every other module uses specific exception types) ensuring `run()` never raises and always returns a summary, with the failure recorded in `errors`, never silently swallowed.
- Built `src/pipeline/runner.py`: argparse CLI matching all three example invocations from the requirements (`--subreddit`, `--keyword`, `--mock`), plus `--limit`, `--output-dir`, `--ai-provider`, `--format`, `--verbose`. Owns all console output (configures logging, prints the terminal report if requested, prints a final human-readable Pipeline Execution Summary) — `pipeline.py` itself stays silent and testable.
- Wrote `tests/test_pipeline.py` — 12 tests: `force_mock` overrides on both factories, `_CountingAIProvider` counting and pass-through, fetch retry (gives up after max attempts; succeeds after transient failures), a full offline `--mock` pipeline run that produces zero insights (MockAIProvider returns non-JSON) but still completes gracefully with an accurate summary, execution-summary JSON validity, `--format terminal` correctly skipping the markdown save, the top-level safety net actually catching an injected unexpected exception (via `monkeypatch`), and a real CLI integration test (`main(["--mock", ...])`) that runs the whole thing end to end at zero cost. **Deliberate test hygiene:** every test that constructs a `Pipeline` explicitly sets `ai_provider="mock"` — this project's real `.env` has a real `GEMINI_API_KEY`, so `ai_provider="auto"` in a test would silently make real, billed API calls during what's supposed to be a fast offline suite. Suite is now 32 tests total (20 prior + 12 new), all passing, all offline except the two real-provider smoke runs done manually below.
- **Ran the full CLI for real, twice.** First, `--mock --limit 3`: completed in ~0ms, correct counts (3 fetched, 0 analyzed — MockAIProvider always fails structure-validation, 6 AI calls — 2 attempts × 3 posts, exactly matching the `_CountingAIProvider` finding above), clean logging, valid saved report and summary. Second, against real Gemini (`gemini-flash-lite-latest`, since the default model's daily quota is *still* exhausted from earlier sessions today): `--keyword refund --limit 4` → 2 posts fetched and analyzed, 2 clusters (correctly not merged into one — genuinely distinct problems), 81.8% verified, 3 AI calls, 11.5s duration, full report and summary saved and inspected.
- **Found and fixed a real bug during the real-provider run, not the mock one** — google-genai's own SDK logging (`"AFC is enabled..."`, raw `HTTP Request: POST ... 200 OK` lines) leaked into the console output at INFO level, because `logging.basicConfig()` configures the *root* logger, which every third-party library's logger inherits from. Fixed by setting the root logger to WARNING and explicitly raising only this project's own `"src"` logger tree to INFO — third-party noise now stays suppressed unless `--verbose` (which sets the root itself to DEBUG, intentionally including third-party output for real debugging).
- **Caught a bug in my own fix before it shipped.** The first version of the logging fix had an early `return` after configuring the root logger at WARNING when `verbose=True` — meaning `--verbose` would have shown *less* output than the default (never actually elevating to DEBUG), backwards from its purpose. Caught by re-reading the code against its own docstring before testing, not by a failing test — fixed before the flag was ever exercised.

### In Progress
None. Task 6 is complete: module built, tested (32 tests passing), run end-to-end in both mock and real-provider modes, one real bug found and fixed via actual execution (the logging leak), one bug in the fix itself caught by review before it shipped. Did not begin Task 7.

### Known Issues
- Fetch retry (`_fetch_with_retry`) retries on *any* `FetcherError`, with no transient-vs-permanent classification — unlike `GeminiProvider`'s retry logic (Task 3), which distinguishes 5xx/429 (retry) from other errors (don't bother). `FetcherError` doesn't currently carry that distinction at all. Simplification, not an oversight — noted rather than building a Fetcher-side error taxonomy that doesn't exist yet for a single extra pipeline stage.
- `analyze_preview.py` (Tasks 3-5's demo script) still exists, unchanged, doing largely the same thing `src/pipeline/runner.py` now does properly. Not removed or consolidated this session — see Questions.
- All Known Issues from the Task 3, 3.1, 4, and 5 entries below still apply except what this session directly addressed.
- The Gemini free-tier daily quota for the default model remains exhausted (five sessions of testing in one day) — `gemini-flash-lite-latest` used again as a testing substitute for the real-provider smoke runs.

### Next Tasks
- Do not begin Task 7 — explicitly out of scope per this session's instruction.
- Decide what to do with `analyze_preview.py` now that `src/pipeline/runner.py` supersedes it (see Questions) — likely deprecate/remove it once the user confirms the new entrypoint covers everything they were using it for.
- When quota resets: re-run the real-provider smoke test against the actual default model to confirm today's numbers hold outside the lite-model substitution — same standing item as every prior task this session.

### Important Decisions
- Extended the two factories with `force_mock` rather than importing concrete classes directly in `pipeline.py` — preserves the "always go through the factory" discipline established since Task 2/3, at the cost of touching two already-reviewed files. Regression-tested immediately as the very first step, before any new code depended on the change.
- Built AI-call counting as an external decorator (`_CountingAIProvider`) rather than instrumenting `GeminiProvider` — keeps Task 3's code untouched, and the decorator pattern means it automatically, correctly counts calls from *any* consumer (Extractor, Aggregator, and anything added later) without each one needing to cooperate.
- `Pipeline.run()`'s one broad `except Exception` is documented as a deliberate, singular exception to this project's "always use specific exception types" pattern — the top-level orchestrator's entire job is "never crash," which is a fundamentally different responsibility than every other module's, and the design explicitly calls out that this is the *only* place in the codebase this pattern is used.
- Chose Python's standard `logging` module over ad hoc `print()` statements for pipeline operational visibility — matches what "add logging" actually means idiomatically in Python, keeps `pipeline.py` silent/testable (consistent with every other non-presentation module in this project), and gives real, standard, timestamped, filterable output almost for free.
- Simplified `ai_provider` to two options (`"auto"`, `"mock"`) rather than three (`"auto"`, `"gemini"`, `"mock"`) — a `"gemini"`-forces-real option would add a choice without adding a capability, since `"auto"` already uses real Gemini whenever it's configured.
- Fixed the logging-noise leak and the bug in that fix's own `--verbose` handling in the same session they were found, rather than filing them for later — both were cheap, both were real correctness issues in an area (operational logging) this task explicitly asked for.

### Questions
- Should `analyze_preview.py` be deprecated/removed now that `src/pipeline/runner.py` does everything it did plus configuration, logging, retry, and execution summaries? Leaning toward "yes, once confirmed the new entrypoint is a strict superset" — not removed unilaterally this session.
- All still-open questions from the Task 3, 3.1, 4, and 5 entries below remain open (total_verified_insights's definition, the stemming gap, the reconciliation-trio partial clustering miss, IDEAS.md's scope, the home-directory git repo).

### Lessons Learned
- **A decorator that implements an existing interface is often the least invasive way to add cross-cutting behavior** (call counting, here) to code you don't want to modify — `_CountingAIProvider` needed zero changes to `GeminiProvider`, `Extractor`, or `Aggregator`, and it's more accurate than manual counting would have been, since it's structurally impossible for it to miss a call.
- **Root-logger configuration affects every dependency's logging, not just your own** — `logging.basicConfig(level=INFO)` looked like a two-line, obviously-correct piece of code and was subtly wrong the moment a real third-party library (google-genai) was in the loop. Only surfaced by actually running against the real provider, not the mock one — the mock path is silent by construction and couldn't have caught this.
- **Catching your own bug by re-reading code against its stated purpose, before running it, is cheaper than catching it by testing** — the `--verbose` early-return bug was found this way, not by a failing test (no test happened to exercise `--verbose` specifically). Worth deliberately re-reading new code against its own docstring once before considering it done, not just relying on tests to catch everything.
- Explicit test hygiene around real credentials in `.env` (forcing `ai_provider="mock"` in every pipeline test) is worth stating as a rule, not just following it by habit — a future session adding a test without thinking about this could silently start spending real quota on every `pytest` run.

### Future Improvements
- If Fetcher-level errors ever need retry-worthiness classification (like `GeminiProvider` already has), that's a real, scoped piece of work — not attempted here.
- Once `analyze_preview.py` is deprecated (see Questions), its removal should be its own small, clearly-labeled cleanup, not folded into an unrelated task.

---

## Session — 2026-07-30 (Task 5: Insight Report Generator)

### Current Objective
Build `src/reporting/` — a pure formatting layer, no AI calls — that turns verified opportunity clusters into a professional business report (Executive Summary, Top Opportunities, Project Health), rendered as pretty terminal output and a saveable Markdown file. Demo the full pipeline: Fetch → Analyze → Cluster → Verify → Generate Report. Stop before Task 6.

### Completed Work
- **Named the two real risks in this task's requirements before writing any code, not after.** "Recommended next action" per opportunity could easily slide into MarketRadar making a build/invest decision for the user — directly against PRD.md §7's non-goals ("does not make investment, funding, or go/no-go decisions") and §10 ("MarketRadar's job ends at presenting well-reasoned, well-sourced findings"). And "supporting quotes" in a polished report could easily end up pulled straight from raw `DiscussionInsight` fields, bypassing Task 4 entirely and making the Verifier decorative rather than load-bearing. Both were treated as hard constraints on the design, not afterthoughts.
- Built `src/reporting/models.py`: `OpportunityReportEntry` (title, opportunity_score, confidence, frequency, verification_rate, has_verification_data, supporting_quotes, representative_discussions, suggested_customer_segment, recommended_next_action), `ProjectHealthSummary` (fetched/analyzed counts, cluster count, verified-insight count, verification percentage, AI provider label, timestamp), `InsightReport` (executive summary + ranked opportunities + health). Reuses `ConfidenceLevel` from `src/insights/models.py` rather than redefining it.
- Built `src/reporting/report_generator.py` (`generate_report(clusters, verification_report, posts, ai_provider_used) -> InsightReport`), enforcing both risks named above structurally, not just by convention:
  - `_collect_verified_quotes` only pulls from `FieldVerification` entries the Verifier marked `VERIFIED` — Partial-status loosely-matched sentences are explicitly excluded, confirmed by a dedicated test.
  - `_recommend_next_action` is a small, fully deterministic decision table (confidence × verification_rate × frequency × has_verification_data → one of five fixed research-oriented templates: "monitor for recurrence," "manually re-read and confirm," "worth a closer manual read," "reasonable next step is reading the full discussions and reaching out," "no verification data available"). None of the five ever says "build" or "invest" — enforced by a test that checks for exactly those forbidden words across every reachable combination of inputs.
  - `_pick_customer_segment` returns one unmodified `user_persona` from the cluster, never a synthesized/merged composite across multiple insights — merging personas would itself be a small act of fabrication with no single discussion actually backing the merged result.
  - Per-cluster `verification_rate` is computed by cross-referencing each cluster's insights against `VerificationReport.results` by `source_post_id`; when no verification data exists for a cluster at all, `has_verification_data=False` is reported explicitly rather than a misleading `0.0` that would look identical to "verified and failed."
- Built `src/reporting/formatter.py`: `format_terminal`, `format_markdown` (proper headers, a Markdown table for project health, blockquotes for verified quotes), and `save_markdown_file` (the only function that touches the filesystem). Every speculative field is labeled inline in both output formats via a single shared `_SPECULATIVE_NOTE` constant, not duplicated free text that could drift out of sync between the two renderers.
- Wrote `tests/test_reporting.py` — 9 tests: per-cluster verification rate computed correctly, Partial-status quotes never leak into the report, missing verification data reported honestly (not as a fabricated 0%), the "never a build/invest verdict" rule checked directly against every reachable recommendation-table branch, customer segment picks the first non-empty persona rather than merging, empty-cluster input handled gracefully, terminal output labels speculative fields, Markdown output has correct structure (headers, table, blockquotes), and `save_markdown_file` actually writes the expected content to a real path (via pytest's `tmp_path`). All pass, all offline. Full suite is now 20 tests (11 from Task 4 + 9 new).
- Extended `src/analyze_preview.py` once more (fifth task built on the same demo script, consistent with the project's established pattern of extending rather than proliferating scripts) with the Generate Report step and a `--no-save` flag; reports save to `output/report_<timestamp>.md` by default (that directory was already anticipated and gitignored back in Task 1).
- **Ran the full pipeline for real** (mock Reddit data; the default Gemini model's daily quota was *still* exhausted from Task 3.1/4's testing, so used `gemini-flash-lite-latest` again as a testing-only override) against 5 mock posts: 5 discussions → 3 opportunity clusters → 72.4% claim-level verification → a complete terminal report and a saved Markdown file, both inspected directly for correctness.
- **Found and fixed a recurrence of the Task 1/2 Windows-console encoding bug** — em-dash characters in `src/reporting/formatter.py`'s `_SPECULATIVE_NOTE` and several `report_generator.py` recommendation/summary strings rendered as `�` in the terminal, exactly as they did the first time this bug was found. Replaced with ASCII (` - `) in every runtime string (docstrings/comments keep em-dashes — same rule as before). Verified the fix with zero API cost by importing the functions directly and checking every reachable string for non-ASCII characters, rather than re-running the full pipeline again.
- **Investigated, rather than silently accepted, a surprising number in the health section: "Total fully-verified insights: 0" despite 72.4% claim-level verification.** Traced this to a real, structural interaction between two already-deliberate Task 4 design choices: `InsightVerificationResult.overall_status` is a worst-case rollup (any single Unverified field makes the whole insight Unverified), and `user_persona` can *by design* never reach `Verified` status (it has no attached quote to confirm — see Task 4). Since every insight has a persona field, no insight can structurally ever reach a fully-`Verified` overall status. This is correct behavior per Task 4's own reasoning, not a bug — but confirmed it's genuinely confusing paired with a 72% headline number, and documented it plainly rather than quietly loosening the definition to produce a more flattering figure.

### In Progress
None. Task 5 is complete: module built, zero AI calls, tests passing, demoed end-to-end with a real saved Markdown file inspected. Did not begin Task 6.

### Known Issues
- **`total_verified_insights` will read as 0 (or very close to it) in almost every real report**, for the structural reason explained above. This is accurate given Task 4's rollup semantics, but likely to read as alarming or broken to a human glancing at the health section next to a much higher claim-level percentage. Not changed this session — flagged as a genuine open question (see Questions) rather than silently patched, since "loosen the definition" and "explain it better in the UI" are different fixes with different tradeoffs.
- Two shared strings (`_SPECULATIVE_NOTE`, recommendation templates) feed both `format_terminal` and `format_markdown`; keeping them ASCII-only for terminal safety means the Markdown file also renders with plain hyphens instead of typographically nicer em-dashes, even though the file itself (UTF-8, not console-codepage-constrained) could safely have used them. Deliberate simplicity/reliability tradeoff, not an oversight — noted for completeness.
- All Known Issues from the Task 3, 3.1, and 4 entries below still apply except what this session directly addressed (the encoding bug, specifically its Task-5-introduced recurrence).
- The Gemini free-tier daily quota for the default model remains exhausted as of this session (cumulative Task 3/3.1/4/5 testing in one day) — `gemini-flash-lite-latest` used again as a testing substitute.

### Next Tasks
- Do not begin Task 6 — explicitly out of scope per this session's instruction.
- Decide what to do about the `total_verified_insights` metric (see Questions) before it's relied on for anything.
- When quota resets: re-run the full pipeline against the real default model to confirm today's numbers hold outside the lite-model substitution.
- Task 8 (final CLI entrypoint, still not built) should probably absorb `analyze_preview.py`'s now-five-task-deep responsibilities into a properly named, polished command rather than continuing to extend a script literally named "preview."

### Important Decisions
- Report generation makes zero AI calls, as required, and every value in the report traces to either a direct pass-through or a deterministic aggregation of already-computed pipeline output — nothing here is newly inferred.
- `_recommend_next_action` is a fixed decision table, not free text, specifically so it can be exhaustively tested against the "never a build/invest verdict" rule — a rule that matters enough to this project's core identity that it needed a real test, not just a docstring promise.
- Report quotes are sourced exclusively from the Verifier's `VERIFIED`-status output, never from raw `DiscussionInsight` fields directly — this was the main design decision that makes Task 4 load-bearing rather than a parallel, disconnected pipeline stage.
- Chose not to synthesize/merge personas across a cluster's insights for "suggested customer segment" — picked one real, unmodified persona instead, even though a merged composite might read as more polished, because a merge would be a small fabrication with no single source backing it.
- Extended `analyze_preview.py` again rather than starting a new script, consistent with every prior task in this project — flagged the resulting name mismatch (a "preview" script doing five tasks' worth of work) as Task 8's problem to resolve, not something to fix as a side effect of this task.
- Did not loosen or reinterpret `total_verified_insights`'s definition to make the number look better once it turned out to structurally read as 0 — reported the finding honestly and left the metric's definition to a deliberate decision, not a quiet patch.

### Questions
- Should `total_verified_insights` be redefined (e.g., "insights with at least N% of fields verified") to be a more useful headline number, or is the strict all-fields-Verified definition the right one and the health section just needs better explanatory text next to it? Not decided — surfaced here rather than resolved unilaterally.
- Should `analyze_preview.py` be renamed/restructured now that it covers the entire pipeline, or is that squarely Task 8's job? Leaning toward "Task 8's job," per the project's established pattern of not doing unscoped renames mid-task.
- All still-open questions from the Task 3, 3.1, and 4 entries below remain open.

### Lessons Learned
- **The Windows-console encoding bug recurred because the ASCII-only rule lived in convention/memory, not in anything enforced.** Worth considering a cheap, permanent guard (e.g., a one-line pytest test that scans all `src/**/*.py` runtime string literals for non-ASCII characters) rather than relying on catching it by eye in each new module — flagged as a Future Improvement rather than built now, since it's a real scope decision (what counts as "runtime" vs "docstring") worth making deliberately.
- Zero-cost verification (importing functions directly and checking string output, rather than re-running the full paid pipeline) was the right call twice in one session — both the encoding fix and the ASCII sweep were confirmed without spending any more of an already-exhausted daily quota.
- A "the numbers don't quite make sense together" reaction (72% verified claims next to 0 verified insights) was worth chasing to a root cause immediately rather than noting vaguely — the actual explanation (persona structurally caps every insight's rollup) is a specific, useful fact about how Task 4 and Task 5 interact, not just a curiosity.

### Future Improvements
- A cheap automated guard against non-ASCII runtime strings (see Lessons Learned) — would have caught this session's encoding bug before a full pipeline run was needed to surface it.
- If `total_verified_insights` is redefined, it should probably become a graduated metric (e.g., percentage of fields verified per insight, not a binary all-or-nothing count) rather than a second binary threshold — flagged, not designed, since this is a real product decision.
- Once Task 8 exists, the report's `--no-save` / output path handling here should probably move there rather than living in a "preview" script.

---

## Session — 2026-07-30 (Task 4: Verification Engine)

### Current Objective
Build a dedicated, independent verification module (`src/verification/`) that re-checks every claim in a `DiscussionInsight` against its original source discussion, produces a per-field verification report, and demos as the last stage of the pipeline: Fetch → Analyze → Cluster → Verify → Print. Not a roadmap continuation — stop before Task 5.

### Completed Work
- **Designed the verification module to make zero AI calls, deliberately.** `Extractor` (Task 3) already does inline, lightweight quote verification before constructing a `DiscussionInsight` — this task explicitly asked for something "completely independent from extraction." Considered whether independence should mean re-verifying with a second AI call, and concluded no: checking an AI's claim against real, immutable source text is a meaningful anti-hallucination signal; checking it with a second AI call just risks one model rubber-stamping the other's mistake, and this project has now hit the real Gemini daily quota twice in one day of testing (Task 3.1), which a zero-AI verifier is immune to entirely.
- Built `src/verification/models.py`: `VerificationStatus` (Verified/Partial/Unverified — new, since it answers a different question than the existing `ConfidenceLevel`), `FieldVerification` (one claim's result: status, confidence, supporting quotes, source discussion IDs), `InsightVerificationResult` (one insight's full set of field checks, plus a worst-case `overall_status` rollup — deliberately pessimistic: one Unverified field makes the whole insight's status Unverified, not averaged away), `VerificationReport` (aggregate counts + `verification_rate`, defined strictly as `verified / total` with Partial claims **not** counted toward the rate, to avoid inflating it).
- Built `src/verification/verifier.py` (`Verifier` class). Designed two honestly different verification ceilings for two honestly different kinds of claims:
  - **Quote-bearing claims** (primary/secondary pain points, supporting evidence, buying signals) — already guaranteed by `Extractor` to be verbatim substrings of their source. Re-confirms this independently via exact substring match; can reach **Verified**. This is real defense-in-depth: it would catch a regression in `Extractor`'s own check, or an insight being verified against the wrong post.
  - **Speculative claims with no attached quote at all** (`user_persona`, `feature_requests` — see `src/insights/models.py`, these were never evidence claims) — checked via a deterministic keyword-overlap heuristic against the source text; can reach at most **Partial** (some textual grounding found), **never Verified** — there is no verbatim quote to confirm, so claiming Verified status for these would itself be a form of overclaiming confidence.
  - Added `VerificationError`, raised if `Verifier.verify()` is ever called with a `DiscussionInsight` whose `source_post_id` doesn't match the given `FetchedPost.id` — verifying a claim against the wrong source could make a hallucination look "confirmed" by textual coincidence, which is worse than not verifying at all.
  - `verify_all(insights, posts)` matches insights to posts by ID and **never silently drops** an insight whose source post isn't in the given list — every field is explicitly reported Unverified with that reason instead, per requirement 5's "never silently discard failures."
- **Chose pytest as the project's test framework** — the first real test suite in this project (previously flagged as an open decision in TODO.md). Chosen over stdlib `unittest` for plain-assert syntax and clearer failure output, consistent with this project's "easy iteration/debugging" architecture goal; no heavy transitive dependencies. Added to `requirements.txt`, installed into `.venv`.
- Wrote `tests/test_verifier.py` — 11 tests covering the failure modes requirement 9 named directly: exact quote match (Verified), quote not in source (Unverified, never fabricated), empty/missing claims, persona/feature-request keyword support (Partial) vs. none (Unverified), verifying against the wrong source (raises), a missing source post never silently dropping the insight, the worst-case `overall_status` rollup, and report-level count/rate aggregation across multiple insights. All pass, all offline, zero API cost — caught one real bug in the test itself (a miscounted expected total) before it ever touched real data.
- Extended `src/analyze_preview.py` (Task 3's demo) rather than creating a new script, per requirement 8's framing of the whole pipeline as one flow: added a `Verify` step after clustering (`Verifier().verify_all(insights, posts)`) and a `_print_verification_summary()` that shows total/Verified/Partial/Unverified counts, the overall rate, and a flagged list of every non-Verified claim (empty fields omitted) for human review.
- **Ran the extended demo for real** (mock Reddit data, real Gemini via `gemini-flash-lite-latest` — the default model's daily quota was still exhausted from Task 3.1's testing) and it worked correctly end to end: 3 discussions → correctly merged into 1 cluster (all three genuinely are the same reconciliation pain point) → 17 total claims verified → 12 Verified (70.6%), 3 Partial (17.6%), 2 Unverified (11.8%).
- **Investigated the two Unverified personas directly rather than assuming the heuristic was simply "too strict."** Found the precise mechanism: claim "Agency owner managing multi-processor payment payouts" vs. source "...it's payouts across three different processors..." — keyword overlap computed at 2/7 = 0.286, against a 0.3 threshold, entirely because "processor" (claim) and "processors" (source) are different tokens under exact-string keyword matching with no stemming. A real, narrow, mechanically-understood limitation — reported honestly rather than silently patched with an unmeasured quick fix (same discipline as Task 3.1's clustering gap).

### In Progress
None. Task 4 is complete: module built, independent from extraction, tested, demoed end-to-end with real data, documented. Did not begin Task 5.

### Known Issues
- **Exact-token keyword matching has no stemming**, causing near-miss false negatives on simple word-form differences (singular/plural, verb tense) — demonstrated precisely above (0.286 vs 0.3 threshold, entirely due to "processor"/"processors"). Not fixed this session; a naive trailing-"s" stripper was considered and deliberately not added without measurement, to avoid introducing new false positives (e.g. on short words) without evidence it actually helps more than it hurts.
- The Gemini free-tier daily quota for the default model (`gemini-flash-latest`) is still exhausted as of this session — `gemini-flash-lite-latest` was used again as a testing-only override, project default unchanged.
- Feature requests extracted by `Extractor` are sometimes degenerate (e.g. the single word "automatically" as a whole "feature request" in this session's test run) — an `Extractor`/prompt quality issue from Task 3, not something Task 4 fixes, but it surfaced while reviewing verification output and is worth noting for whoever revisits the extraction prompt.
- "Claims needing review" in the demo's printed summary is unbounded — fine at this dataset's scale (7 items, ~17-40 claims), would need pagination/truncation at real scale.
- All Known Issues from the Task 3 and Task 3.1 entries below still apply except what this session directly addressed.

### Next Tasks
- Do not begin Task 5 — explicitly out of scope per this session's instruction.
- If the stemming gap turns out to matter in real use: the cheapest next step is a minimal, conservative pluralization normalizer (strip trailing "s" only on words above a length threshold), measured against real verification output before being trusted, not assumed to be strictly better.
- When quota resets: re-run this same demo against the real default model to confirm the verification numbers hold outside the lite-model substitution.
- Consider whether `tests/` needs a `conftest.py` or package structure as it grows — currently a single flat file, fine at this size.

### Important Decisions
- **Verification makes zero AI calls, by design, not by omission.** This is the single most important architectural choice in this task: an AI-based "verifier" would be checking one model's output using another model's judgment, with no independent ground truth — deterministic text matching against the actual source is what makes verification meaningful here. Also immune to the quota problems that disrupted Task 3.1's testing.
- Gave quote-bearing and non-quote-bearing claims genuinely different verification ceilings (Verified vs. Partial-at-best) rather than forcing every field through the same check — this is more honest than either pretending personas can be "verified" like a quote, or refusing to assess them at all.
- Chose a deliberately pessimistic `overall_status` rollup (worst-case, not majority or average) — a report that averaged away a single Unverified claim among nine Verified ones would undersell exactly the kind of risk this module exists to surface.
- Chose pytest over unittest now that a real testing need existed, rather than deferring the framework decision again — this was flagged as open in TODO.md since Task 2 and this was the first task where "add tests" was an explicit requirement, not just a someday-item.
- Extended the existing `analyze_preview.py` rather than creating a new demo script — the task explicitly frames Fetch→Analyze→Cluster→Verify as one pipeline, and the project has consistently built each task's demo as an extension of the previous one rather than proliferating scripts.
- Investigated the two Unverified personas to a precise, quantified cause before writing them up, rather than reporting "the heuristic missed these" — same discipline applied in Task 3.1 to the clustering gap, and it turned a vague weakness into a specific, actionable one (a stemming gap with an exact numeric near-miss), which is far more useful to a future session than an impression would be.

### Questions
- Is the stemming/pluralization gap worth fixing now, or is it acceptable as a known, narrow limitation for a personal tool at this scale? Leaning toward "acceptable for now, fix if it recurs on real data" — not a unilateral decision, see the architecture review's recommendation.
- Should `Extractor`'s prompt (Task 3) be revisited to stop producing degenerate one-word "feature requests"? Out of scope for this task, flagged for whoever next touches `src/insights/prompts.py`.

### Lessons Learned
- **Don't guess why a heuristic result looks wrong — measure it.** "The persona verification seems too strict" would have been a much weaker finding than "processor vs. processors missed a 0.3 threshold by 0.014" — the second is immediately actionable (or immediately dismissible as not worth fixing); the first just invites unfocused tinkering.
- Building the test suite alongside the module (not after) caught a bug in the *test* itself (a miscounted total) rather than a bug in the code — a reminder that test correctness needs the same scrutiny as production code correctness, especially the arithmetic in assertions.
- The "two ceilings for two kinds of claims" design only became obvious by re-reading the existing `DiscussionInsight` model closely (persona/feature_requests genuinely have no evidence_quote field) rather than assuming every field could be verified the same way — worth re-reading existing types carefully before designing something that consumes them.

### Future Improvements
- A minimal, measured stemming pass for the keyword-overlap heuristic (see Next Tasks) — explicitly not built without evidence it helps.
- If verification ever needs to run against much larger discussion volumes, the "claims needing review" report will need pagination or filtering (e.g. by confidence, by field type) rather than printing every flagged claim.
- Consider whether `VerificationReport` should support filtering/exporting (e.g. "show me only Unverified primary pain points") once there's an actual downstream consumer (the Report Writer, not yet built) that would use it.

---

## Session — 2026-07-30 (Task 3.1: Fix Opportunity Clustering)

*Split into its own entry rather than appended to the (already very large) main 2026-07-30 entry below — see that entry's own Future Improvements note.*

### Current Objective
The architecture review at the end of Task 3 found that `Aggregator`'s clustering did not work: 7 mock discussions produced 7 clusters, known duplicates never merged, measured Jaccard similarity was near zero. Fix clustering until duplicates reliably merge — nothing else. Not a roadmap continuation.

### Completed Work
- **1. Investigated the root cause** (confirming the Task 3 review's diagnosis, not re-deriving it from scratch): the AI generates an independent one-sentence paraphrase of each discussion's pain point, and two paraphrases of the same real problem often share almost no literal vocabulary even when a human instantly recognizes them as the same issue. Keyword/Jaccard overlap has no access to semantics, so no amount of threshold tuning or keyword-source widening (already tried in Task 3) can fix it — confirmed quantitatively (0.0–0.067 similarity vs. 0.35 threshold on known duplicates).
- **2. Evaluated the four suggested approaches:**
  - *Embeddings* — most "principled"/measurable, but requires a new `AIProvider.embed_text()` capability (new abstract method, new GeminiProvider logic, new MockAIProvider stub) plus vector-similarity code. More correct in theory, meaningfully more to build.
  - *Local/library-based semantic matching* (e.g. sentence-transformers) — rejected outright: pulls in a heavy dependency (torch) directly against the project's stated "low cost, simplicity" priorities for a personal tool, for a problem the existing AI provider can already solve.
  - *Better normalization* (stemming, synonym expansion) — rejected: the gap is semantic (different words, same meaning), not surface-form (same word, different spelling); normalization doesn't touch the actual problem.
  - *AI-generated canonical pain point / AI-assisted grouping* — chosen. Reuses the exact `AIProvider.generate_text()` interface already built and proven in Task 3, zero new abstract methods, zero new dependencies, one extra call per aggregation run (not per-pair), and directly addresses the semantic gap by delegating the judgment to a model that has already demonstrated (via extraction) it understands these discussions.
- **3. Implemented the chosen approach** in `src/insights/aggregator.py`: `Aggregator` now takes an optional `AIProvider`. `_ai_cluster()` builds one batched prompt (`build_clustering_prompt` in `prompts.py`, kept separate from logic per the Task 3 convention) listing every discussion's pain point + evidence, and asks Gemini to return `{"clusters": [{"label", "member_indices"}]}`. `_parse_clustering_response()` validates and repairs the response so every index ends up in exactly one group regardless of AI imperfection — duplicate indices deduped (first wins), out-of-range indices dropped, any index the AI never placed becomes its own singleton — verified against six synthetic edge cases (malformed JSON, missing key, duplicate/out-of-range/missing indices, markdown-fenced output) with zero API cost before ever calling the real model. The original keyword-overlap heuristic is kept, unchanged, as `_lexical_cluster()` — a deterministic fallback used only when no AI provider is given or the AI call/response fails.
- **Found and fixed a real observability bug while testing:** the first full-dataset test run after implementing AI-assisted clustering *still* showed 7 clusters / 0 merges — but investigating directly (calling the clustering prompt+response in isolation) revealed the real cause was **the Gemini free-tier daily quota (20 requests/day for the default model) had been exhausted by the day's cumulative testing**, not a bug in the new clustering logic. `Aggregator` was silently falling back to the already-known-insufficient lexical path with zero indication this had happened — exactly the kind of silent failure this project's principles exist to prevent, just inside test tooling instead of product output. Fixed: `Aggregator` now records `last_method` ("ai"/"lexical_fallback") and `last_fallback_reason` as public attributes (not a print — kept business logic and presentation separated, consistent with how `Extractor` raises rather than prints); `analyze_preview.py` now prints a `[WARN]` when fallback occurs and labels which method actually produced the shown clusters.
- **4. Measured the improvement using the same mock dataset**, after switching to `gemini-flash-lite-latest` for this one measurement (confirmed via direct API probing to have separate, unexhausted quota; the project's checked-in default, `gemini-flash-latest`, is unchanged — this was a temporary env-var override for testing only, not a config change):
  - **Before (established in the Task 3 review, unchanged):** 7 discussions → 7 clusters. 0 of 2 known-duplicate pairs merged. Reconciliation trio's pairwise similarity: 0.0–0.067 vs. 0.35 threshold.
  - **After (AI-assisted, this session, `last_method == "ai"` confirmed):** 7 discussions → **5 clusters**. The no-code pair (post-005, comment-006) merged completely and correctly (2/2). The reconciliation trio partially merged: post-001 and comment-003 correctly joined into one cluster; comment-002 (framed by the extractor around "internal scripts breaking across multiple processors" rather than "accounting reconciliation") stayed a singleton rather than joining them.
  - **Merge accuracy:** item-level — 4 of the 5 known-duplicate items ended up grouped with at least one true sibling (80%). Pairwise — of the 4 true "should-merge" pairs (3 within the reconciliation trio, 1 within the no-code pair), 2 merged correctly (50%).
  - **False merges: 0.** No predicted cluster mixes items from different true underlying pain points — onboarding (post-004) and scheduler (post-007) both correctly stayed singleton, and nothing unrelated got pulled into either merged group. Precision on every merge that did happen: 100%.
  - **Remaining weakness:** comment-002 not joining the reconciliation cluster is a real, demonstrated, partial miss — not fixed further this session (see Important Decisions for why).

### In Progress
None. Task 3.1 is complete: investigated, four approaches evaluated on paper, one implemented, measured against the same dataset with before/after numbers, and a recommendation is due below. Did not begin Task 4.

### Known Issues
- **The Gemini free-tier daily quota for the default model (`gemini-flash-latest`, currently resolving to `gemini-3.6-flash`) is exhausted for today**, consumed by cumulative testing across Task 3 and Task 3.1. `check_connections.py` and `analyze_preview.py` will likely show `[FAIL]`/errors against the real key until it resets. `gemini-flash-lite-latest` had separate quota as of this session, confirmed by direct probing — not switched as the permanent default, since that's a real product decision (quality/cost tradeoff) the user should make deliberately, not one that should happen as a side effect of hitting a quota wall during testing.
- Clustering is now demonstrably much better, not perfect: the reconciliation trio only 2/3 merged. Root cause is plausible and specific (the extractor's own paraphrase of comment-002 emphasizes a different angle — processor/script management vs. accounting reconciliation — than the other two), not a mystery, but not fixed.
- All the Known Issues carried over from the Task 3 entry below still apply except the two this session directly addressed (clustering quality, silent-fallback observability) — see that entry for the rest (no offline test path for the Insight Engine, asymmetric fail-loud/fail-silent verification behavior, etc.).

### Next Tasks
- Do not begin Task 4 — explicitly out of scope per this session's instruction.
- When quota resets: re-run the same measurement against the real default model (`gemini-flash-lite-latest` was only a testing substitute) to confirm the numbers hold, not just on the lite model.
- If the comment-002 partial miss matters enough to chase further: the likely fix is giving the clustering prompt more than just the primary description + evidence (e.g. secondary pain points too, the same widening that didn't help the lexical approach but might help the AI reason about the connection) — untried this session, flagged not attempted.
- Consider whether `gemini-flash-lite-latest` (confirmed working, separate quota, presumably cheaper) is actually the better default for this personal, cost-conscious tool rather than `gemini-flash-latest` — a real product decision to put in front of the user, not something to silently switch to because it was convenient during testing.

### Important Decisions
- Chose AI-assisted batch clustering (one call per aggregation run) over embeddings, local semantic matching, or better normalization — reused existing, already-proven infrastructure (`AIProvider.generate_text()`), added no new dependency, and directly targets the demonstrated root cause (semantic, not lexical, similarity gap) rather than a proxy for it.
- Deliberately validated the response parser against synthetic edge cases (malformed JSON, missing/duplicate/out-of-range indices) before spending a single real API call on it — cheap, and it meant the eventual real-data test was checking the actual clustering quality, not accidentally debugging a parsing bug at the same time.
- **Did not chase the remaining 1-of-5 partial miss (comment-002) to 100%.** The improvement achieved (0% → 80% item-level recall, 0% false merges) is significant and directly measured, per the task's own bar ("choose the simplest approach that significantly improves clustering quality"), and further tuning risks overfitting to this one 7-item mock dataset rather than generalizing. Reported the gap honestly instead of hiding it or over-optimizing against a tiny sample.
- Kept the original keyword-overlap clusterer as a fallback rather than deleting it — it's not good enough to be primary (proven), but it's still better than crashing or returning zero clusters when no AI provider is available at all.
- Fixed the silent-fallback observability gap as part of this task rather than filing it as a separate future item — it was directly responsible for the first "after" measurement being wrong (looked like the fix didn't work; actually the fix never ran, quota exhaustion did), so it was blocking correctness measurement, not a nice-to-have.
- Used a temporary env-var model override (`gemini-flash-lite-latest`) for measurement only, explicitly not changed in `config.py` or `.env` — switching the project's real default is a cost/quality decision for the user, not something that should happen as a byproduct of a testing session hitting a quota wall.

### Questions
- Is switching the default model to `gemini-flash-lite-latest` (confirmed working today, presumably cheaper/lower-quality than the flash tier) worth doing deliberately, given how easily the flash tier's daily quota gets exhausted during active development? Not decided — surfaced here for the user.
- Is the comment-002-style partial miss worth chasing further (see Next Tasks), or is 80% item-level recall / 0% false-merge rate an acceptable steady state for a personal tool at this scale? Recommendation given below; not a unilateral decision.

### Lessons Learned
- **A "the fix didn't work" result should be investigated directly before being believed** — the first re-test after implementing AI-assisted clustering looked exactly like the pre-fix failure (7 clusters, 0 merges), which could easily have been read as "the new approach doesn't work either." Isolating the clustering call directly (prompt + raw response, outside the full pipeline) found the real cause in one step: a quota error, not a logic error. Always separate "does the new code run" from "does the new code do the right thing" when a result looks suspiciously identical to the old failure.
- **Silent fallbacks actively harm measurement, not just production behavior.** The whole reason this session had to redo a measurement was that a fallback happened invisibly during a test run. Any fallback path in this codebase should now be treated as needing visible reporting by default, not just this one.
- **Cheap validation before expensive validation, in that order, remains worth doing every time** — testing the parser against six free synthetic cases first meant zero uncertainty about whether a bad "after" number was a parsing bug or a clustering quality issue once real API testing began.
- Free-tier daily quotas are a real, easy-to-hit constraint during iterative AI development, not just a production scaling concern — worth remembering for a personal tool where "just test it again" is the default workflow.

### Future Improvements
- If clustering quality needs to improve further, richer context in the clustering prompt (secondary pain points, not just primary) is the cheapest untried next step.
- Consider a small local cache of AI clustering responses keyed by input hash, purely to avoid re-spending quota re-running the same measurement during iterative development — would help exactly the workflow that hit today's quota wall. Not built; flagged as a real, recurring friction point.

---

## Session — 2026-07-30

### Current Objective
Move from planning into implementation: define a one-week, single-user MVP scope, design its technical architecture, get the repo properly hosted on GitHub, and start building Task 1 (project setup & external connections).

### Completed Work
- Designed a one-week personal MVP scope (single source, on-demand trigger, quote-verified extraction, in-run recurrence grouping, three-tier confidence label, saved Markdown report) — deliberately narrower than ROADMAP.md's full Phase 1, cut for a one-week solo build.
- Designed the MVP's technical architecture: Python, PRAW (Reddit, read-only official API), Claude API (extraction/grouping), plain string-match quote verification (deterministic, no AI), flat-file storage (JSON + Markdown), single CLI entrypoint, no database/server/hosting.
- Fixed the git repo root problem (previously the repo root was the user's home directory, flagged repeatedly since the initial documentation session): initialized a new repo scoped to `MarketRadar/` itself, renamed default branch to `main`, committed all 8 planning documents as the root commit.
- Pushed the repo to GitHub at `github.com/rr7572854820-gif/MarketRader`.
- Built Task 1 (Project Setup & External Connections): `.gitignore`, `requirements.txt` (praw, anthropic, python-dotenv only), `.env.example`, `src/config.py` (env var loader/validator), `src/check_connections.py` (verifies Reddit + Claude auth, never logs secret values).
- Adapted Task 1 after the user said they don't have and don't want to create a Reddit API key: made Reddit fully optional. `Config.reddit_configured` is now a computed property (true only if both `REDDIT_CLIENT_ID` and `REDDIT_CLIENT_SECRET` are set); `check_connections.py` skips the Reddit check (not a failure) when they're absent, and only Claude is required to pass.
- Found and read a `TODO.md` in the project root that neither of us had written in this conversation — apparently produced by a separate session or the user directly. It correctly flagged that ROADMAP.md's Phase 0 ("no software") and PRD.md §13 (source selection deliberately deferred to Milestone 0.1) hadn't formally happened before Task 1's Reddit-specific code was written. Resolved this explicitly rather than silently proceeding or silently blocking: treated the user's personal-MVP reframing (this same day) as a deliberate, recorded exception to Phase 0's ceremony for a personal tool — see Important Decisions.
- Built Task 2 (Data Fetcher): `src/models.py` (`FetchedPost`/`FetchQuery` — the one shape every source, current or future, must return), `src/fetchers/base.py` (`Fetcher` ABC + `FetcherError`), `src/fetchers/reddit_fetcher.py` (real read-only PRAW fetch of posts + top-level comments), `src/fetchers/mock_fetcher.py` (7 realistic, clearly-labeled sample posts/comments about business pain points — Stripe/QuickBooks reconciliation, no-code conditional logic, meeting-scheduler timezones, etc.), `src/fetchers/__init__.py` (`get_fetcher(config)` factory choosing real vs. mock with no caller-side branching), `src/fetch_preview.py` (CLI to print fetched posts).
- Actually ran the code rather than just writing it: created a local `.venv`, installed `requirements.txt`, and exercised `fetch_preview.py` and `check_connections.py` against the real `.env` (which has no Reddit or Anthropic credentials set). This surfaced two real bugs, both fixed before considering Task 2 done:
  1. `load_config()` hard-required `ANTHROPIC_API_KEY`, which meant the Fetcher — which has nothing to do with Claude — couldn't even be tested without a Claude key. Fixed by making Claude optional at load time too (mirroring Reddit): added `Config.anthropic_configured`, and moved the "Claude is actually required" enforcement into `check_connections.py` itself, which now fails loudly and specifically only when *it* needs Claude.
  2. Em-dash and ellipsis characters in `print()` output (not in docstrings/comments, which don't matter) rendered as `�` on the Windows console due to an encoding mismatch. Replaced with plain ASCII (`-`, `...`) in every actual print statement across `check_connections.py` and `fetch_preview.py`.
- Verified mock-mode fetch end-to-end: default fetch returns all 7 sample items, `--keyword refund` correctly narrows to 2, `--limit` truncates correctly, and `check_connections.py` now reports `[SKIP]` for Reddit and a clear `[FAIL]` for Claude (matching the real, unfilled `.env` state) instead of crashing.
- Updated `TODO.md` to mark Tasks 1–2 complete, record the sequencing-gap resolution, and lay out Tasks 3–9 (Extractor through dogfood pass) as the concrete remaining path.
- **Replaced Anthropic/Claude with Google Gemini as the AI provider**, at the user's request (no budget for Claude's paid API; Gemini has a usable free tier). Built a proper provider abstraction rather than a direct swap: `src/ai/base.py` (`AIProvider` ABC + `AIProviderError`), `src/ai/gemini_provider.py` (real, via the `google-genai` SDK), `src/ai/mock_provider.py` (offline testing — always "succeeds," returns a fixed, clearly-prefixed fake response), `src/ai/__init__.py` (`get_ai_provider(config)` factory, mirroring `get_fetcher`'s pattern exactly). `check_connections.py` now verifies the AI provider through this abstraction (`get_ai_provider(config).check_connection()`) and never imports `google.genai` directly, satisfying "the rest of the app must never directly call Gemini" literally, not just in spirit.
- Removed Anthropic entirely from `config.py` (`anthropic_api_key`/`claude_model`/`anthropic_configured`/`DEFAULT_CLAUDE_MODEL` all gone), replaced with `gemini_api_key`/`gemini_model`/`gemini_configured`/`DEFAULT_GEMINI_MODEL = "gemini-2.5-flash"`. Updated `requirements.txt` (removed `anthropic`, added `google-genai`), `.env.example`, and the real `.env` (rewrote the Anthropic section to Gemini in place via `sed`, without ever reading the actual secret values into context — confirmed the old `ANTHROPIC_API_KEY`/`CLAUDE_MODEL` were empty first, so nothing was lost).
- Verified the refactor's isolation actually holds, not just in theory: re-ran `fetch_preview.py` after the entire AI-provider swap with zero changes needed to it — proof that Task 2's Fetcher/pipeline separation from the AI layer works as designed.
- Verified `MockAIProvider` end-to-end (selected automatically with no `GEMINI_API_KEY` set, `check_connection()` raises nothing, `generate_text()` returns the clearly-labeled fake response) and reinstalled the venv (`pip uninstall anthropic`, `pip install -r requirements.txt` picking up `google-genai`). All files byte-compile cleanly.
- Added an empty `GEMINI_API_KEY=` line to the user's real `.env`, ready for them to paste a real key into (asked them to do it directly in the editor, not in chat). They added it and confirmed.
- **Ran the real, live Gemini verification this task explicitly gated on — and it surfaced one more real bug.** `python -m src.check_connections` passed (`[OK] Gemini authentication succeeded`), but the first real `generate_text()` call failed: `404 NOT_FOUND — This model models/gemini-2.5-flash is no longer available to new users.` Confirmed this wasn't a fluke by calling `client.models.list()` directly — `gemini-2.5-flash` is still *listed* as available, but generation against it is blocked for this (newer) API key regardless. Listing a model is not the same as being able to use it.
- Fixed by testing candidate models directly against the real key before picking one: `gemini-flash-latest` (an alias Google keeps pointed at their current recommended fast model, rather than a dated version string) generated correctly on the first real call. Changed `DEFAULT_GEMINI_MODEL` in `src/config.py` from `"gemini-2.5-flash"` to `"gemini-flash-latest"`, and updated `.env.example`'s comment to explain the `list()`-vs-`generate()` gap so a future session doesn't hit the same surprise blind.
- Re-verified everything through the actual project code (not ad hoc SDK calls) after the fix: `check_connections.py` → `[OK]`; `get_ai_provider(config)` returns a `GeminiProvider` instance (`isinstance` checks against both `GeminiProvider` and `MockAIProvider` confirm this explicitly, not just by class name string); `provider.generate_text("Reply with exactly: MarketRadar Gemini connection successful.")` returned exactly `'MarketRadar Gemini connection successful.'` — an exact match, not a paraphrase; the response does not start with the `MockAIProvider`'s `[MOCK...]` prefix, confirming mock mode is not in play. No secret value was printed anywhere during any of this — only presence/length checks and the (non-secret) generated response text.
- **Built Task 3 (AI Insight Engine)** — new `src/insights/` package: `models.py` (`DiscussionInsight`, `PainPoint`, `OpportunityCluster`, plus `ConfidenceLevel`/`Sentiment` enums — every field explicitly documented as either evidence-backed or speculative inference, see below), `prompts.py` (the extraction prompt as a pure string-builder, no logic), `extractor.py` (`Extractor.extract(post) -> DiscussionInsight`, JSON parsing with one retry on malformed output, and inline verbatim-quote verification against the source text — never calls `google.genai` directly, only `AIProvider.generate_text()`), `aggregator.py` (`Aggregator.aggregate(insights) -> List[OpportunityCluster]`, deterministic keyword-overlap clustering, no AI call). Added `src/analyze_preview.py` (Fetch → Analyze → Print demo CLI, `python -m src.analyze_preview COMMUNITY`).
- **Named a real tension in the request before building it, rather than silently complying or refusing.** Two required fields — `opportunity_score (1-100)` and `startup_opportunity` — sit directly against README.md's "MarketRadar is NOT a startup idea generator" and PRD.md §7's non-goals ("does not generate startup ideas," "market attractiveness signal is an input, not a verdict"), and against ROADMAP.md's own sequencing (this is Phase 5 Milestone 5.3 material, gated behind multi-source corroboration and time-series history, not Task 3 on one ungrounded post). Since this is the user's own personal tool, treated this as their call to make, not a veto — but implemented it with the same discipline the rest of the project uses: every speculative field (`user_persona`, `startup_opportunity`, `opportunity_score`, `urgency_score`) is structurally labeled `SPECULATIVE` in the data model's own docstrings and in every printed output line, never presented unlabeled as a finding. Evidence-bearing fields (`evidence_quote`, `supporting_evidence`, `buying_signals`) are held to the project's actual non-fabrication standard: verified as verbatim substrings of the source text before a `DiscussionInsight` can even be constructed; if the *primary* pain point's quote can't be verified, the whole extraction fails loudly (`InsightExtractionError`) rather than presenting an unverified primary claim.
- **Added reliability features inside `GeminiProvider` only** (never in the Extractor, keeping "Gemini isolated behind AIProvider" real): confirmed the actual `google.genai.errors` class hierarchy against the installed SDK (`APIError` base, `ClientError`/`ServerError` siblings, `.code` holds the HTTP status — checked programmatically, not assumed) before writing retry logic. `generate_text()` now retries up to 3 attempts with exponential backoff on `ServerError` (5xx) or `ClientError` with `.code == 429` (rate limiting); any other error (bad key, invalid model, etc.) fails immediately since retrying wouldn't help. `Extractor.extract()` separately retries once on malformed/invalid JSON output (a different failure mode — the AI call succeeded but returned unparseable content) — the two retry loops are independent and don't double up.
- **Ran the demo for real** (mock Reddit data + the already-verified real Gemini connection — a legitimate, low-cost, honest end-to-end test, not a synthetic one) and it surfaced two more real findings:
  1. `MockAIProvider` returns plain text, not JSON, so every extraction would fail against it — correctly, since it can't verify a quote for a source text it never sees. Deliberately did **not** teach `MockAIProvider` the insights JSON schema to work around this: that would leak `src/insights/`'s schema knowledge down into `src/ai/`, which is supposed to be generic provider infrastructure independent of what's built on top of it — exactly the layering violation this whole architecture exists to prevent. Documented this as a real, named gap instead (see Known Issues) — there is currently no way to exercise the Insight Engine's parsing/validation offline without a real Gemini key.
  2. **The clustering/dedup requirement demonstrably does not work as built.** The mock dataset deliberately contains two known-duplicate pairs (3 posts about Stripe/QuickBooks/bookkeeper reconciliation pain from different authors; 2 posts about no-code tools failing on conditional logic). Running all 7 through the real pipeline produced **7 separate clusters — zero merges, on either pair.** Quantified it rather than eyeballing: measured Jaccard similarity between the three known-duplicate reconciliation insights' keyword sets came out to 0.0–0.067, against the clustering threshold of 0.35 — off by roughly an order of magnitude, not a near-miss. Root cause: the AI generates an independent one-sentence paraphrase per discussion, and two paraphrases of the same underlying problem often share almost no literal vocabulary even when a human would instantly recognize them as the same issue. Widened the keyword source (from just the primary description to primary + secondary descriptions + all evidence quotes) as a cheap, harmless improvement before concluding this — it did not close the gap. Precision was fine throughout (nothing unrelated got wrongly merged); recall was effectively zero. This is exactly the "genuinely hard judgment problem" ROADMAP.md Milestone 3.2 already predicted for cross-source dedup — confirmed here, empirically, one phase earlier than expected, on a single source. Did not chase further fixes (e.g., AI-assisted clustering, embeddings) — that's real scope creep into Task 4/5 territory and explicitly out of bounds per "do not begin Task 4."

### In Progress
None. Task 3 is built, tested against the real Gemini connection, and stopped exactly at the boundary requested ("do not begin Task 4").

### Known Issues
- The user's home directory (`C:\Users\RISHABH RAJPUT`) still has its own separate, empty git repo (no commits). Left untouched — out of scope for this project.
- IDEAS.md's scope ambiguity (feature ideas about MarketRadar itself vs. market opportunities its research surfaces) — still not explicitly confirmed by the user, though still not urgent since IDEAS.md hasn't been used yet.
- The real Reddit path (`RedditFetcher`) has not been exercised against a live account — only its construction/import has been implicitly verified. It should get a real test the first time Reddit credentials are actually added.
- A local `.venv/` exists in the project (created for testing, already gitignored) — harmless, but worth knowing it's there.
- `gemini-flash-latest` is an alias, not a pinned version — Google can repoint it to a different underlying model at any time. Fine for a personal tool that wants "whatever currently works," but if reproducible behavior across time ever matters, pin a specific dated model name in `GEMINI_MODEL` instead.
- **`MockAIProvider` cannot exercise the Insight Engine offline** — it returns plain text, not schema-conforming JSON, so every extraction against it correctly fails verification. No schema-aware test double exists yet (deliberately not built into `src/ai/` — see Completed Work). Offline/CI testing of `src/insights/` will need a purpose-built fake `AIProvider` scoped to insights tests specifically, once a test suite exists.
- **Clustering/deduplication does not work in practice, only in theory.** `Aggregator` is deterministic keyword-overlap clustering; measured against real AI output on two known-duplicate pairs in the mock dataset, it merged neither (0 for 2), with similarity scores an order of magnitude below threshold. See Completed Work for the full measurement. `OpportunityCluster.occurrence_count` should currently be read as "how many discussions independently *worded their pain point identically enough*," not "how many discussions describe this problem" — those are not the same thing yet.
- `opportunity_score` and `startup_opportunity` exist and are labeled `SPECULATIVE` everywhere they're printed, but nothing yet stops a future piece of code from reading `insight.opportunity_score` and using it as if it were a real ranking signal without also carrying that label along. The labeling is currently a documentation/print-formatting convention, not a type-system guarantee.

### Next Tasks
- Do not begin Task 4 (Verifier) — explicitly out of scope for this session per the user's own instruction.
- When Task 4 does start: it can build on `Extractor`'s existing inline quote-verification rather than duplicating it, but should add what Task 3 deliberately didn't — cross-post verification, and a real decision about whether `secondary_pain_points`/`buying_signals`/`supporting_evidence` need the same "fail loudly" treatment currently reserved for the primary pain point (right now they're silently dropped if unverifiable, not raised).
- If clustering quality matters before Task 5 formally addresses it (ROADMAP Milestone 3.2), the cheapest next experiment would be AI-assisted clustering (one extra `generate_text` call given all primary pain point descriptions at once, asking Gemini to group them) rather than further keyword-heuristic tuning — but that's a real design decision (added cost/latency/dependency per aggregation run) worth surfacing to the user rather than just doing.
- Whenever the user wants to test real Reddit data, add `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET` to `.env` — no code change needed.

### Important Decisions
- Chose to make Reddit optional via a config-level mock-mode flag rather than removing the Reddit dependency entirely — keeps the real path fully wired (just add credentials later) instead of requiring rework when the user is ready for real data.
- Resolved the sequencing-gap flag raised by the discovered `TODO.md` by treating it as answered, not overridden: the user's explicit personal-MVP reframing (this same day, this same conversation) already constitutes a conscious decision to bypass ROADMAP Phase 0's formal ceremony for personal use. Recorded this explicitly here and in TODO.md rather than either silently complying with the new request or silently blocking on the old flag.
- Extended the "optional config, consumer-checked" pattern from Reddit to the AI provider as well, once testing showed the alternative (hard-requiring a paid key to even test the Fetcher) was an unnecessary coupling between unrelated concerns. `load_config()` now never fails; only specific consumers that need a specific credential check for it and fail loudly themselves.
- Designed the Fetcher as an abstract `Fetcher` interface + `FetcherError`, with `RedditFetcher`/`MockFetcher` as swappable implementations behind a single `get_fetcher(config)` factory, and a single shared `FetchedPost` shape — so a future Hacker News/Product Hunt/GitHub source is one new class + one factory branch, never a change to code that consumes fetched data.
- Applied the exact same pattern to the AI provider (`AIProvider`/`AIProviderError`/`get_ai_provider`) when swapping Anthropic for Gemini, rather than doing a direct find-and-replace of Claude calls with Gemini calls — this is what makes "Anthropic, OpenAI, Grok pluggable later" actually true rather than aspirational, and it's why `fetch_preview.py` needed zero changes despite a full AI-backend swap.
- Deliberately did *not* build a concrete `AnthropicProvider`/`OpenAIProvider`/`GrokProvider` now, even though the interface supports them — per CLAUDE.md's "no dead code, no speculative abstractions," the interface being extensible is enough; building unused providers would be scope creep with nothing to test them against.
- Chose ASCII-only characters in all runtime `print()` output after hitting real Windows console mojibake — docstrings/comments can keep em-dashes freely since those are read as source text, not printed.
- Refused to treat "the code is written" as "the task is done" given the user's explicit "do not begin Task 3 until the Gemini integration is fully working" — stopped short of Task 3 and reported the real blocker (no key yet) rather than assuming success.
- Named the opportunity_score/startup_opportunity tension against README/PRD non-goals explicitly, then implemented it anyway with structural labeling rather than either quietly complying (which would let a "startup idea generator" output creep into the project's own code without ever being named as such) or quietly refusing (which would override the user's explicit, repeated authority over their own personal tool). This is the same pattern CLAUDE.md §8 prescribes generally, applied to a concrete case.
- Deliberately did not teach `MockAIProvider` the insights JSON schema when mock-mode extraction predictably failed — chose to name the gap instead of quietly coupling two layers that are supposed to stay independent. A quick hack would have made the demo output look more complete; it would also have made the architecture worse in a way that's easy to forget was ever a shortcut.
- Deliberately did not chase a better clustering algorithm after confirming the keyword-overlap approach fails on real duplicate pairs — widened the keyword source once (cheap, harmless, evidence-based), measured the result, and stopped rather than iterating further into what is explicitly Task 4/5 territory (and instructed out of scope for this session). Reporting a demonstrated limitation honestly is more valuable here than an undisclosed patch that happens to work on this one demo.

### Questions
- Should the home-directory git repo be removed at some point, or left alone indefinitely? Still unresolved.
- Should IDEAS.md's scope be explicitly confirmed before it's ever actually used?
- Is keyword-overlap clustering acceptable to ship as "good enough for now" given it demonstrably doesn't merge known duplicates, or should real clustering (AI-assisted or embedding-based) be pulled forward before Task 4/5 gets there on schedule? Not decided — flagged for the user in the architecture review.

### Lessons Learned
- When a user pushes back on a setup requirement (here: not wanting a Reddit API key, then not being able to afford Claude), the better fix is usually to make the dependency optional/pluggable at the architecture level rather than a one-off patch — the Reddit mock-mode pattern turned out to generalize cleanly to swapping the entire AI provider later, because it was built as a real abstraction the first time, not a special case.
- Keeping secret-handling logic in exactly one module (`config.py`) paid off a third time — removing Anthropic and adding Gemini was a contained, mechanical change, not a hunt across the codebase.
- **Actually running the code caught two real bugs that reading it carefully did not** (from Task 2) — the Claude/Fetcher coupling and the Windows console encoding issue were both invisible on inspection and obvious the moment the commands were executed. Don't skip the "run it" step even for code that looks correct.
- When a task has an explicit hard gate ("do not begin Task 3 until X works"), build everything up to that gate, verify what can be verified without the missing piece (mock mode here), and then actually stop and ask — rather than assuming the missing external dependency will show up and reporting premature success.
- Finding an unexpected file (`TODO.md`) mid-task is worth reading fully and reconciling explicitly, not working around silently — it surfaced a real, valid process concern that deserved an on-the-record answer either way.
- **A model "being listed" by an API is not the same as being usable by a given key** — `client.models.list()` returning a model doesn't guarantee `generate_content()` will accept it for that specific key. The only real test of "does this model work" is calling it, not enumerating what's technically listed. Worth remembering before ever hardcoding a specific model name again without testing it against the real key first.
- When a default value (a model name, here) turns out to be wrong, don't just patch the symptom — test the fix against the real dependency before committing to it, the same way the original default should have been tested rather than assumed.
- **Don't trust a clustering/dedup heuristic until it's tested against a case specifically designed to require it.** The mock dataset's duplicate pairs existed since Task 2 for exactly this reason and it was worth actually running the full 7-item set, not just a 3-item sample, before calling the aggregation requirement done — the failure was invisible on smaller runs and in code review, only visible once real output was inspected against known ground truth.
- Quantifying a suspected weakness (computing actual Jaccard scores) turned "clustering seems weak" into "clustering is off by roughly an order of magnitude" — a much more useful finding for a review than an impression, and cheap to produce since it reused already-generated data instead of new API calls.

### Future Improvements
- The mock dataset's two known-duplicate pairs did their job: they're what caught the clustering failure. Worth keeping that property (deliberately-duplicated mock items) as the dataset grows, rather than only adding thematically distinct items.
- If/when Task 4 or 5 revisits clustering, AI-assisted grouping (feed all primary pain point descriptions to Gemini in one call) is a cheap experiment worth trying before anything heavier (embeddings, a real NLP library) — flagged in Next Tasks above, not decided.
- This session's SESSION.md entry has grown very large (covers Tasks 1, 2, the AI provider swap, and Task 3 all under one 2026-07-30 header). Worth reconsidering the "one file, newest-first" journal structure soon, per the standing note in the 2026-07-29 entry's Future Improvements below — this entry is close to the point where finding anything in it requires Ctrl+F, not reading.

## Session — 2026-07-29

### Current Objective
Stand up MarketRadar's foundational documentation from a blank project: the source-of-truth product documents (README, PRD, CLAUDE.md), then the supporting planning documents (ROADMAP, SESSION, IDEAS) and research templates — without writing any code, architecture, or implementation detail.

### Completed Work
- Diagnosed and fixed a structural problem: several project files (`README.md`, `PRD.md`, `CLAUDE.md`, and later `IDEAS.md`, `ROADMAP.md`, `SESSION.md`) had been accidentally created as empty directories rather than actual Markdown files, likely from an earlier `mkdir` mistake. Verified each was genuinely empty before removing the directory and replacing it with a real file, so no prior work was lost.
- Wrote [README.md](./README.md): what MarketRadar is, who it's for, why it exists, long-term vision, project philosophy, and future direction.
- Wrote [PRD.md](./PRD.md): full product requirements — executive summary, problem statement, target users, personas, jobs-to-be-done, goals, non-goals, MVP definition, long-term vision, product principles, success metrics, risks, constraints, assumptions, open questions, and future expansion.
- Wrote [CLAUDE.md](./CLAUDE.md): permanent project memory for future Claude Code sessions — mission, development philosophy, product principles, always/never rules, architecture principles, coding standards, communication style, code review approach, planning approach, assumption-challenging approach, git workflow, documentation standards, and a project-wide Definition of Done.
- Fixed the structure a second time after `IDEAS.md`, `ROADMAP.md`, and `SESSION.md` were found to have the same empty-directory problem — removed and recreated as real, empty files, verified as files (not directories), and confirmed `README.md`/`PRD.md`/`CLAUDE.md` were left untouched throughout.
- Wrote [ROADMAP.md](./ROADMAP.md): seven phases (Research, MVP, Validation, Expansion, Automation, Intelligence, Scale), broken into small, independently valuable milestones, each with goal, rationale, expected outcome, dependencies, complexity, risks, and a Definition of Done — deliberately excluding architecture, technology choices, and calendar estimates.
- Wrote this file (SESSION.md) as the project's ongoing journal template and initial entry.
- Wrote [IDEAS.md](./IDEAS.md): a twelve-dimension scoring framework (Problem Severity, Market Size, Evidence Strength, Frequency, Growth Trend, Competition, Execution Difficulty, Technical Risk, Revenue Potential, Founder Fit, Long-Term Moat, Validation Status), four decision categories, an entry template, an empty active backlog and rejected-ideas log, and one clearly-labeled fictional example entry to demonstrate the format without being mistaken for real evidence.
- Resolved the `research/` naming mismatch: removed the two mistyped, empty placeholder directories (`hackersnews.md`, `productionhunt.md`) alongside the three correctly-named ones (`reddit.md`, `github.md`, `competitors.md`) — all confirmed empty immediately before removal — and created five real files with the corrected names (`reddit.md`, `hackernews.md`, `producthunt.md`, `github.md`, `competitors.md`).
- Wrote all five research templates (`research/reddit.md`, `research/hackernews.md`, `research/producthunt.md`, `research/github.md`, `research/competitors.md`), each covering purpose, data source, collection method, signals, pain-point extraction, evidence collection, recurring-complaint tracking, competitor tracking, market scoring, risk analysis, confidence score, source links, a validation checklist, weekly/monthly summary formats, and open sections for observations and open questions. Deliberately left every "findings" section empty (`[Not yet collected]`) rather than seeded with invented example data.
- Self-reviewed all eight documents for internal consistency and found two real issues, both fixed: (1) ROADMAP.md Milestone 6.1 incorrectly attributed a PRD.md §3 point to CLAUDE.md §4 — corrected the citation; (2) all five research templates' "Market Scoring" sections incorrectly pointed to IDEAS.md as the framework for scoring discovered market opportunities, contradicting IDEAS.md's own explicit scope (which covers MarketRadar's own product features, not the pain points its research surfaces) — corrected all five to point instead to ROADMAP.md's future Milestone 5.3 (Market Attractiveness Scoring), with an explicit "do not compute or imply a score" instruction until that capability exists.
- Committed the above as `6edbdc7 Finish MVP planning`, then in a later pass the same day added "Task 1" scaffolding (commit `91efc12 Complete initial MarketRadar setup`): `.gitignore`, `requirements.txt` (praw, anthropic, python-dotenv), `.env.example`, `src/__init__.py`, `src/config.py` (single-point `load_config()` env-var loader, fails loudly via `ConfigError` if Reddit/Anthropic credentials are missing, never returns partial secrets), and `src/check_connections.py` (verifies Reddit read-only auth and Claude API auth actually work; deliberately does no fetching/extraction; never prints a credential value or raw exception message, only the exception type name).
- End-of-session review pass (this entry, added retroactively): ran the actual project — confirmed git repo root is correctly scoped to the `MarketRadar` folder (the "Known Issues" entry below about a misplaced home-directory repo root is now stale/resolved), confirmed `.env` is gitignored and still unfilled (identical to `.env.example`, so no real secret has ever existed on disk or in git history), confirmed no fabricated data exists in any research template or IDEAS.md entry, and confirmed no venv/dependencies are installed yet (`python -m src.check_connections` currently fails on `ModuleNotFoundError`, not on real credential logic — expected, not a bug). Created [TODO.md](./TODO.md) as the prioritized next-steps list.

### In Progress
None — all eight documents requested this session (ROADMAP.md, SESSION.md, IDEAS.md, and the five research templates) are complete and self-reviewed.

### Known Issues
- ~~This machine's git repository root was found to be the user's entire home directory...~~ **Resolved as of the end-of-session review**: `git rev-parse --show-toplevel` now correctly resolves to the `MarketRadar` folder itself, and `git ls-files` only lists this project's own files. No action needed.
- `docs/` exists with no defined purpose yet — not addressed this session. (`src/` now has a defined purpose — see Completed Work.)
- IDEAS.md's scoring dimensions (Market Size, Growth Trend, Competition, Revenue Potential, Founder Fit, etc.) closely resemble a startup-opportunity scorecard, which created a real ambiguity about whether the framework was meant to evaluate MarketRadar's own product features or the market opportunities MarketRadar's research surfaces for founders — the latter would conflict with MarketRadar not being an idea generator. Resolved by scoping IDEAS.md explicitly to MarketRadar's own features/product ideas, but this interpretation has not been explicitly confirmed by the user — see Questions.
- **New:** `src/config.py` and `src/check_connections.py` are real, working software, and ROADMAP.md Phase 0 states explicitly "Phase 0 produces no software." Separately, PRD.md §13 lists "which initial sources offer the strongest evidence-to-effort ratio" as a sourcing decision *deliberately deferred* from the PRD, and ROADMAP.md Milestone 0.1 (Source Feasibility Assessment) — which is supposed to produce that decision — has not been done. Yet `.env.example` and `check_connections.py` already commit specifically to Reddit as the first source. This isn't fabrication or scope creep in output (the code makes no evidentiary claims), but it is a sequencing gap worth the user's explicit sign-off rather than quietly proceeding as if Milestone 0.1 were already satisfied — flagged in TODO.md.
- No venv exists and `requirements.txt` is not installed anywhere on this machine yet; `python -m src.check_connections` currently fails at `ModuleNotFoundError: No module named 'dotenv'`, before it ever reaches credential logic. Expected, not a regression — just the next setup step.

### Next Tasks
See [TODO.md](./TODO.md) for the full prioritized list. Highlights:
- Confirm with the user whether IDEAS.md's intended scope (feature/product ideas about MarketRadar itself) matches their intent, given the dimension set could also read as a market-opportunity scorecard (see Known Issues and Questions).
- Populate IDEAS.md's backlog with real, evidence-backed candidates once any actual research (Phase 0 of ROADMAP.md) begins — the framework should exist before any idea does.
- Set up a venv, install `requirements.txt`, fill in real Reddit + Anthropic credentials in `.env`, and get `python -m src.check_connections` to print "All connections verified."
- Get explicit user sign-off on the Reddit-as-first-source sequencing gap noted in Known Issues before writing any extraction code against it.

### Important Decisions
- Confirmed README.md, PRD.md, and CLAUDE.md are the project's source of truth; all subsequently written documents (ROADMAP, SESSION, IDEAS, research templates) must stay consistent with them rather than introduce new, conflicting scope or philosophy.
- Chose not to fabricate example research data inside the research templates — templates are structural/instructional only, consistent with the project's core "never fabricate evidence" rule extending even to planning documents. The one example entry in IDEAS.md is explicitly labeled fictional/illustrative for the same reason.
- Treated the empty-directory files as safe to delete only after individually verifying each was genuinely empty first, rather than assuming based on their appearance in a prior listing — applied this same discipline a second time to the `research/` directory before removing the mistyped placeholders.
- Scoped IDEAS.md to MarketRadar's own product/feature ideas, not to the market opportunities the product's research surfaces, and corrected the research templates to match — see Known Issues for the ambiguity this resolved.

### Questions
- Does IDEAS.md's scope (MarketRadar's own features, not discovered market opportunities) match what was intended, given the dimension set reads like it could apply to either?
- Should the misplaced home-directory git repository be corrected before any further work, given that it currently makes `git status` inside this project misleading?
- What should `docs/` and `src/` be used for, and when should that be defined — is `docs/` intended to hold anything beyond the top-level planning documents already in the project root?

### Lessons Learned
- Verify file-vs-directory state explicitly (not just from a prior `ls` in conversation memory) immediately before any destructive operation, even a low-risk one like removing an apparently-empty directory — state can change between when it was last observed and when it's acted on.
- When a user-specified target filename doesn't match an existing artifact's name, treat that as a signal to flag explicitly rather than silently picking one interpretation — naming mismatches are cheap to surface and expensive to leave ambiguous.
- Cross-referencing documents by section number (e.g., "PRD.md §3") is only valuable if the citations are actually checked against the source — a self-review pass caught one wrong citation and one real scope contradiction that would have shipped otherwise; this kind of check is worth doing explicitly, not just assumed correct while writing.

### Future Improvements
- Once real research begins, consider whether this journal should stay a single growing file or move to one-file-per-session once it becomes unwieldy — not a decision to make preemptively.
- Consider whether SESSION.md entries should eventually be summarized periodically (e.g., a "project history" rollup) once enough sessions accumulate that reading the full log becomes impractical.
