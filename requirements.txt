# Reddit + Gemini client libraries and .env loading.
# Verification, grouping, and reporting each add their own dependencies
# later; do not add anything here that isn't used yet.

praw>=7.7
google-genai>=0.3
python-dotenv>=1.0
