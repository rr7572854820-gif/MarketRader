# Copy to .env.local and adjust if the API isn't running on the default
# host/port. This is only the build-time default - it can also be
# overridden at runtime per-browser from the Settings page, which is
# handier when testing against a non-default port without a rebuild.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
